import type { OutputFormat, ResizeFit } from "../types";

export const DEFAULT_OUTPUT_FORMAT: OutputFormat = "original";
export const DEFAULT_FIT: ResizeFit = "inside";
export const DEFAULT_QUALITY = 82;
export const DEFAULT_PNG_COMPRESSION = 9;
export const DEFAULT_SUFFIX = "-compressed";
